<Window x:Class="거래플랜.Desktop.App.Views.RentalContractEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="렌탈계약서 작성"
        Width="1520"
        Height="940"
        WindowStartupLocation="CenterOwner"
        Background="#151F2E">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#EAF2FF"/>
            <Setter Property="FontFamily" Value="맑은 고딕"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#D9E6F5"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#4A6382"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="FontFamily" Value="맑은 고딕"/>
        </Style>
        <Style x:Key="EditableTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Background" Value="#C8D5E5"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                    <Setter Property="IsReadOnly" Value="False"/>
                    <Setter Property="Background" Value="#D9E6F5"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#D9E6F5"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#4A6382"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="FontFamily" Value="맑은 고딕"/>
        </Style>
        <Style x:Key="EditableComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="IsEnabled" Value="False"/>
            <Setter Property="Background" Value="#C8D5E5"/>
            <Style.Triggers>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType=Window}}" Value="True"/>
                        <Condition Binding="{Binding DataContext.CanEditManagementOffice, RelativeSource={RelativeSource AncestorType=Window}}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="IsEnabled" Value="True"/>
                    <Setter Property="Background" Value="#D9E6F5"/>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="Background" Value="#D9E6F5"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#4A6382"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="맑은 고딕"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Padding" Value="8,1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="SelectedDateFormat" Value="Short"/>
            <Setter Property="IsEnabled" Value="False"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                    <Setter Property="IsEnabled" Value="True"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="{x:Type DatePickerTextBox}">
            <Setter Property="Background" Value="#D9E6F5"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Padding" Value="8,0,10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="DatePickerButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource UnifiedDatePickerButtonStyle}"/>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#5D7593"/>
            <Setter Property="Background" Value="#435A72"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="MinWidth" Value="108"/>
            <Setter Property="FontFamily" Value="Malgun Gothic"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#062445" Padding="12,10">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="렌탈계약서 작성" FontSize="22" FontWeight="Bold"/>
                    <TextBlock Text="엑셀 렌탈계약서 전체 내용을 기준으로 계약서를 작성하고 수정합니다." Foreground="#9FB3C8" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Content="{Binding ToggleEditButtonText}" Background="#6A1B9A" BorderBrush="#6A1B9A" Margin="0,0,8,0" Command="{Binding ToggleEditModeCommand}"/>
                    <Button Content="미리보기 갱신" Background="#1565C0" BorderBrush="#1565C0" Margin="0,0,8,0" Command="{Binding RefreshPreviewCommand}"/>
                    <Button Content="닫기" Command="{Binding CloseCommand}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Border DockPanel.Dock="Bottom" Background="#122038" BorderBrush="#2E4060" BorderThickness="0,1,0,0" Padding="10">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#A5D6A7"/>
        </Border>

        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="560"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border Background="#1A2B4A" BorderBrush="#2E4060" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                        <StackPanel>
                            <TextBlock Text="계약 기본 정보" FontWeight="Bold" Foreground="#4FC3F7" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="110"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="거래처"/>
                                <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource EditableTextBoxStyle}" Text="{Binding TenantName, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Grid.Row="1" Text="사업자번호" Margin="0,6,0,0"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Margin="0,6,0,0" Style="{StaticResource EditableTextBoxStyle}" Text="{Binding TenantBusinessNumber, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Grid.Row="2" Text="주소" Margin="0,6,0,0"/>
                                <TextBox Grid.Row="2" Grid.Column="1" Margin="0,6,0,0" Style="{StaticResource EditableTextBoxStyle}" Text="{Binding TenantAddress, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Grid.Row="3" Text="담당자" Margin="0,6,0,0"/>
                                <TextBox Grid.Row="3" Grid.Column="1" Margin="0,6,0,0" Style="{StaticResource EditableTextBoxStyle}" Text="{Binding TenantRepresentative, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Grid.Row="4" Text="관리업체" Margin="0,6,0,0"/>
                                <ComboBox Grid.Row="4"
                                          Grid.Column="1"
                                          Margin="0,6,0,0"
                                          Style="{StaticResource EditableComboBoxStyle}"
                                          ItemsSource="{Binding ManagementOfficeOptions}"
                                          DisplayMemberPath="DisplayName"
                                          SelectedValuePath="Value"
                                          SelectedValue="{Binding SelectedManagementOfficeCode, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Grid.Row="5" Text="업체 사업자번호" Margin="0,6,0,0"/>
                                <TextBox Grid.Row="5" Grid.Column="1" Margin="0,6,0,0" Style="{StaticResource EditableTextBoxStyle}" Text="{Binding CompanyBusinessNumber, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Grid.Row="6" Text="업체 주소" Margin="0,6,0,0"/>
                                <TextBox Grid.Row="6" Grid.Column="1" Margin="0,6,0,0" Style="{StaticResource EditableTextBoxStyle}" Text="{Binding CompanyAddress, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Grid.Row="7" Text="대표자" Margin="0,6,0,0"/>
                                <TextBox Grid.Row="7" Grid.Column="1" Margin="0,6,0,0" Style="{StaticResource EditableTextBoxStyle}" Text="{Binding CompanyRepresentative, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Grid.Row="8" Text="전화" Margin="0,6,0,0"/>
                                <TextBox Grid.Row="8" Grid.Column="1" Margin="0,6,0,0" Style="{StaticResource EditableTextBoxStyle}" Text="{Binding CompanyContactNumber, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Grid.Row="9" Text="팩스" Margin="0,6,0,0"/>
                                <TextBox Grid.Row="9" Grid.Column="1" Margin="0,6,0,0" Style="{StaticResource EditableTextBoxStyle}" Text="{Binding CompanyFaxNumber, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Grid.Row="10" Text="제품관리번호" Margin="0,6,0,0"/>
                                <TextBox Grid.Row="10" Grid.Column="1" Margin="0,6,0,0" Style="{StaticResource EditableTextBoxStyle}" Text="{Binding ManagementNumber, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Grid.Row="11" Text="품명" Margin="0,6,0,0"/>
                                <TextBox Grid.Row="11" Grid.Column="1" Margin="0,6,0,0" Style="{StaticResource EditableTextBoxStyle}" Text="{Binding ItemName, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Grid.Row="12" Text="기계번호" Margin="0,6,0,0"/>
                                <TextBox Grid.Row="12" Grid.Column="1" Margin="0,6,0,0" Style="{StaticResource EditableTextBoxStyle}" Text="{Binding MachineNumber, UpdateSourceTrigger=PropertyChanged}"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Background="#1A2B4A" BorderBrush="#2E4060" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                        <StackPanel>
                            <TextBlock Text="계약 일정 / 요금" FontWeight="Bold" Foreground="#4FC3F7" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="110"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="계약일"/>
                                <DatePicker Grid.Row="0" Grid.Column="1" SelectedDate="{Binding ContractDate}">
                                    <DatePicker.Resources>
                                        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource DatePickerButtonStyle}"/>
                                    </DatePicker.Resources>
                                </DatePicker>
                                <TextBlock Grid.Row="1" Text="계약시작" Margin="0,6,0,0"/>
                                <DatePicker Grid.Row="1" Grid.Column="1" Margin="0,6,0,0" SelectedDate="{Binding ContractStartDate}">
                                    <DatePicker.Resources>
                                        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource DatePickerButtonStyle}"/>
                                    </DatePicker.Resources>
                                </DatePicker>
                                <TextBlock Grid.Row="2" Text="계약만료" Margin="0,6,0,0"/>
                                <DatePicker Grid.Row="2" Grid.Column="1" Margin="0,6,0,0" SelectedDate="{Binding ContractEndDate}">
                                    <DatePicker.Resources>
                                        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource DatePickerButtonStyle}"/>
                                    </DatePicker.Resources>
                                </DatePicker>
                                <TextBlock Grid.Row="3" Text="설치위치" Margin="0,6,0,0"/>
                                <TextBox Grid.Row="3" Grid.Column="1" Margin="0,6,0,0" Style="{StaticResource EditableTextBoxStyle}" Text="{Binding InstallLocation, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Grid.Row="4" Text="보증금" Margin="0,6,0,0"/>
                                <TextBox Grid.Row="4" Grid.Column="1" Margin="0,6,0,0" Style="{StaticResource EditableTextBoxStyle}" Text="{Binding DepositText, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Grid.Row="5" Text="월 렌탈요금" Margin="0,6,0,0"/>
                                <TextBox Grid.Row="5" Grid.Column="1" Margin="0,6,0,0" Style="{StaticResource EditableTextBoxStyle}" Text="{Binding MonthlyFee, UpdateSourceTrigger=PropertyChanged}"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Background="#1A2B4A" BorderBrush="#2E4060" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                        <StackPanel>
                            <TextBlock Text="서문 / 체결문구" FontWeight="Bold" Foreground="#4FC3F7" Margin="0,0,0,8"/>
                            <TextBox Style="{StaticResource EditableTextBoxStyle}" Text="{Binding IntroText, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="96"/>
                            <TextBox Style="{StaticResource EditableTextBoxStyle}" Margin="0,8,0,0" Text="{Binding ClosingLine1, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="52"/>
                            <TextBox Style="{StaticResource EditableTextBoxStyle}" Margin="0,8,0,0" Text="{Binding ClosingLine2, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="52"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#1A2B4A" BorderBrush="#2E4060" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                        <StackPanel>
                            <TextBlock Text="계약 조항" FontWeight="Bold" Foreground="#4FC3F7" Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding Clauses}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#102039" BorderBrush="#2E4060" BorderThickness="1" CornerRadius="4" Padding="8" Margin="0,0,0,8">
                                            <StackPanel>
                                                <DockPanel Margin="0,0,0,6">
                                                    <TextBox Width="92"
                                                             DockPanel.Dock="Left"
                                                             Style="{StaticResource EditableTextBoxStyle}"
                                                             Text="{Binding Number, UpdateSourceTrigger=PropertyChanged}"
                                                             Margin="0,0,8,0"/>
                                                    <TextBox Style="{StaticResource EditableTextBoxStyle}" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>
                                                </DockPanel>
                                                <TextBox Style="{StaticResource EditableTextBoxStyle}"
                                                         Text="{Binding Body, UpdateSourceTrigger=PropertyChanged}"
                                                         AcceptsReturn="True"
                                                         TextWrapping="Wrap"
                                                         VerticalScrollBarVisibility="Auto"
                                                         Height="120"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <Border Background="#1A2B4A" BorderBrush="#2E4060" BorderThickness="1" CornerRadius="6" Padding="10">
                        <StackPanel>
                            <TextBlock Text="특약 사항" FontWeight="Bold" Foreground="#4FC3F7" Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding SpecialTerms}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBox Style="{StaticResource EditableTextBoxStyle}"
                                                 Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                                                 AcceptsReturn="True"
                                                 TextWrapping="Wrap"
                                                 VerticalScrollBarVisibility="Auto"
                                                 Height="62"
                                                 Margin="0,0,0,8"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <GridSplitter Grid.Column="1" Width="8" HorizontalAlignment="Stretch" Background="#1A2B4A" ShowsPreview="True"/>

            <Border Grid.Column="2" Background="#0F1D31" BorderBrush="#2E4060" BorderThickness="1" Padding="6">
                <DocumentViewer Document="{Binding PreviewDocument}" />
            </Border>
        </Grid>
    </DockPanel>
</Window>
