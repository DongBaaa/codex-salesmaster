<Window x:Class="SalesMaster.Desktop.App.Views.InvoiceHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="이전기록불러오기"
        Height="620"
        Width="980"
        WindowStartupLocation="CenterOwner"
        Background="#152235"
        KeyDown="Window_KeyDown">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="해당 거래처의 이전 전표를 선택하세요. 여러 건을 고르면 날짜순, 각 전표의 품목순으로 불러옵니다."
                   Foreground="#EAF2FF"
                   FontSize="13"
                   Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchBox"
                     Grid.Column="0"
                     Background="White"
                     Foreground="Black"
                     Padding="10,6"
                     TextChanged="SearchBox_TextChanged"/>
            <Button Grid.Column="1"
                    Content="전체선택"
                    Margin="8,0,6,0"
                    Padding="14,6"
                    Click="SelectAllButton_Click"/>
            <Button Grid.Column="2"
                    Content="전체해제"
                    Padding="14,6"
                    Click="ClearSelectionButton_Click"/>
        </Grid>

        <DataGrid x:Name="HistoryGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="False"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  Background="#1E2D40"
                  Foreground="White"
                  AlternatingRowBackground="#223754"
                  RowBackground="#1E2D40"
                  BorderBrush="#2E4060"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#2E4060"
                  HeadersVisibility="Column">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#1A2B4A"/>
                    <Setter Property="Foreground" Value="#4FC3F7"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="8,5"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="선택" Binding="{Binding IsSelected, Mode=TwoWay}" Width="58"/>
                <DataGridTextColumn Header="일자" Binding="{Binding InvoiceDateDisplay}" Width="110" IsReadOnly="True"/>
                <DataGridTextColumn Header="전표번호" Binding="{Binding InvoiceNumber}" Width="160" IsReadOnly="True"/>
                <DataGridTextColumn Header="합계" Binding="{Binding TotalAmount, StringFormat=N0}" Width="110" IsReadOnly="True"/>
                <DataGridTextColumn Header="항목수" Binding="{Binding LineCount}" Width="70" IsReadOnly="True"/>
                <DataGridTextColumn Header="항목요약" Binding="{Binding Summary}" Width="2*" IsReadOnly="True"/>
                <DataGridTextColumn Header="메모" Binding="{Binding Memo}" Width="*" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="불러오기"
                    Padding="18,7"
                    Margin="0,0,8,0"
                    Click="ConfirmButton_Click"/>
            <Button Content="취소"
                    Padding="18,7"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
