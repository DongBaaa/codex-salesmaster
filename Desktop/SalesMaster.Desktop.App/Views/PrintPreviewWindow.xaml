<Window x:Class="SalesMaster.Desktop.App.Views.PrintPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="거래명세서 미리보기"
        Width="1180"
        Height="860"
        WindowStartupLocation="CenterOwner"
        Background="#151F2E">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#1A2B4A" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding StatusMessage}" Foreground="White" VerticalAlignment="Center" FontFamily="맑은 고딕"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="확대" Width="70" Margin="0,0,6,0" Command="{Binding ZoomInCommand}"/>
                    <Button Content="축소" Width="70" Margin="0,0,6,0" Command="{Binding ZoomOutCommand}"/>
                    <Button Content="인쇄" Width="90" Margin="0,0,6,0" Command="{Binding PrintCommand}"/>
                    <Button Content="닫기" Width="90" Command="{Binding CloseCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Margin="10" Background="#0F1D31" BorderBrush="#2E4060" BorderThickness="1" Padding="6">
            <DocumentViewer Document="{Binding Document}" Zoom="{Binding Zoom, Mode=TwoWay}" />
        </Border>
    </DockPanel>
</Window>
