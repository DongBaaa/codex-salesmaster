<Window x:Class="SalesMaster.Desktop.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:SalesMaster.Desktop.App.Converters"
        Title="코덱스 레거시 판매관리" Height="860" Width="1440"
        WindowStartupLocation="CenterScreen"
        Background="#151F2E"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <conv:DecimalToStringConverter x:Key="DecimalToString"/>
        <conv:DateOnlyConverter x:Key="DateOnlyConverter"/>
        <conv:VoucherTypeConverter x:Key="VoucherTypeConverter"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#EAF2FF"/>
            <Setter Property="FontFamily" Value="맑은 고딕"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#D9E6F5"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#4A6382"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="CaretBrush" Value="Black"/>
            <Setter Property="MinHeight" Value="34"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#D9E6F5"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#4A6382"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="MinHeight" Value="34"/>
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="Background" Value="#D9E6F5"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#4A6382"/>
            <Setter Property="MinHeight" Value="34"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="FontFamily" Value="맑은 고딕"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1E2D40"/>
            <Setter Property="Foreground" Value="#EAF2FF"/>
            <Setter Property="BorderBrush" Value="#2E4060"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#2E4060"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="AlternatingRowBackground" Value="#223754"/>
            <Setter Property="RowBackground" Value="#1E2D40"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1A2B4A"/>
            <Setter Property="Foreground" Value="#4FC3F7"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="#151F2E"/>
            <Setter Property="BorderBrush" Value="#2E4060"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="#1A2B4A"/>
            <Setter Property="Foreground" Value="#9FB3C8"/>
            <Setter Property="BorderBrush" Value="#2E4060"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1,1,1,0"
                                CornerRadius="4,4,0,0"
                                Margin="0,0,2,0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#29466E"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="TabBorder" Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#1A2B4A" Padding="16,10" BorderBrush="#2E4060" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="코덱스 레거시 판매관리" FontSize="22" FontWeight="Bold" Foreground="#4FC3F7" Margin="0,0,14,0"/>
                    <TextBlock Text="{Binding SyncStatus}" Foreground="#9FB3C8" VerticalAlignment="Center" FontSize="11"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="재고관리" Background="#1B5E20" Margin="0,0,6,0" Click="InventoryButton_Click"/>
                    <Button Content="거래처등록" Background="#8E24AA" Margin="0,0,6,0" Click="CustomerEditButton_Click"/>
                    <Button Content="판매작성" Background="#1565C0" Margin="0,0,6,0" Click="SalesButton_Click"/>
                    <Button Content="수금지불" Background="#00897B" Margin="0,0,10,0" Click="PaymentButton_Click"/>
                    <TextBlock Text="{Binding CurrentUserDisplay}" Foreground="#9FB3C8" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Button Content="동기화" Background="#37474F" Command="{Binding ForceSyncCommand}" Margin="0,0,6,0"/>
                    <Button Content="백업" Background="#455A64" Command="{Binding BackupNowCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border DockPanel.Dock="Top" Background="#122038" Padding="12,10" BorderBrush="#2E4060" BorderThickness="0,0,0,1">
            <UniformGrid Columns="4">
                <Border Background="#1A2B4A" Margin="0,0,8,0" CornerRadius="6" Padding="10"><StackPanel><TextBlock Text="이번달 매출" Foreground="#9FB3C8" FontSize="11"/><TextBlock Text="{Binding DashboardMonthlySales, StringFormat={}{0:N0} 원}" Foreground="#4FC3F7" FontWeight="Bold" FontSize="17"/></StackPanel></Border>
                <Border Background="#1A2B4A" Margin="0,0,8,0" CornerRadius="6" Padding="10"><StackPanel><TextBlock Text="미수금" Foreground="#9FB3C8" FontSize="11"/><TextBlock Text="{Binding DashboardReceivable, StringFormat={}{0:N0} 원}" Foreground="#EF9A9A" FontWeight="Bold" FontSize="17"/></StackPanel></Border>
                <Border Background="#1A2B4A" Margin="0,0,8,0" CornerRadius="6" Padding="10"><StackPanel><TextBlock Text="거래처 수" Foreground="#9FB3C8" FontSize="11"/><TextBlock Text="{Binding DashboardCustomerCount, StringFormat={}{0:N0} 곳}" Foreground="White" FontWeight="Bold" FontSize="17"/></StackPanel></Border>
                <Border Background="#1A2B4A" Margin="0,0,8,0" CornerRadius="6" Padding="10"><StackPanel><TextBlock Text="안전재고 경고" Foreground="#9FB3C8" FontSize="11"/><TextBlock Text="{Binding DashboardSafetyStockAlerts, StringFormat={}{0:N0} 건}" Foreground="#EF9A9A" FontWeight="Bold" FontSize="17"/></StackPanel></Border>
            </UniformGrid>
        </Border>

        <Border DockPanel.Dock="Bottom" Background="#0D1B2A" Padding="10,6"><TextBlock Text="{Binding SyncStatus}" Foreground="#9FB3C8" FontSize="11"/></Border>

        <TabControl SelectedIndex="{Binding SelectedTabIndex}" Margin="10">
            <TabItem Header="전표 목록">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Background="#1A2B4A" Padding="10" CornerRadius="6" Margin="0,0,0,8">
                        <WrapPanel>
                            <TextBlock Text="기간" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <DatePicker SelectedDate="{Binding FilterFrom, Converter={StaticResource DateOnlyConverter}}" Width="140" Margin="0,0,6,0"/>
                            <TextBlock Text="~" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <DatePicker SelectedDate="{Binding FilterTo, Converter={StaticResource DateOnlyConverter}}" Width="140" Margin="0,0,12,0"/>
                            <Button Content="조회" Background="#37474F" Command="{Binding LoadInvoiceListCommand}" Margin="0,0,16,0"/>
                            <Button Content="판매작성" Background="#1565C0" Click="SalesToolbarButton_Click" Margin="0,0,6,0"/>
                            <Button Content="수정" Background="#8E24AA" Click="EditInvoiceButton_Click" Margin="0,0,6,0"/>
                            <Button Content="삭제" Background="#E53935" Command="{Binding DeleteInvoiceCommand}" Margin="0,0,6,0"/>
                            <Button Content="수금 입력" Background="#00897B" Click="PaymentEntryButton_Click" Margin="0,0,6,0"/>
                            <Button Content="거래명세서 인쇄[F9]" Background="#1E88E5" Command="{Binding PrintStatementCommand}"/>
                        </WrapPanel>
                    </Border>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="280"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="300"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="#1A2B4A" CornerRadius="6">
                            <DockPanel>
                                <Border DockPanel.Dock="Top" Background="#102039" CornerRadius="6,6,0,0" Padding="10,8">
                                    <StackPanel>
                                        <TextBlock Text="거래처 목록" Foreground="#4FC3F7" FontWeight="Bold"/>
                                        <TextBox Text="{Binding CustomerFilterText, UpdateSourceTrigger=PropertyChanged}" Margin="0,8,0,0"/>
                                        <Button Content="전체 보기" Background="#2E4158" Command="{Binding ClearCustomerFilterCommand}" Margin="0,8,0,0"/>
                                    </StackPanel>
                                </Border>
                                <ListBox ItemsSource="{Binding FilteredCustomers}" SelectedItem="{Binding SelectedCustomerFilter}" Background="Transparent" BorderThickness="0">
                                    <ListBox.ContextMenu>
                                        <ContextMenu><MenuItem Header="거래처 수정" Click="CustomerEditContextMenu_Click"/></ContextMenu>
                                    </ListBox.ContextMenu>
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Padding" Value="8,6"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter Property="Background" Value="White"/>
                                                    <Setter Property="Foreground" Value="Black"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate><DataTemplate><TextBlock Text="{Binding NameOriginal}" Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=Foreground}" TextTrimming="CharacterEllipsis"/></DataTemplate></ListBox.ItemTemplate>
                                </ListBox>
                            </DockPanel>
                        </Border>

                        <GridSplitter Grid.Column="1" Width="8" Background="#0D1B2A"/>

                        <Grid Grid.Column="2">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="8"/>
                                <RowDefinition Height="220"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Background="#1A2B4A" CornerRadius="6">
                                <DataGrid ItemsSource="{Binding InvoiceRows}" SelectedItem="{Binding SelectedInvoiceRow}" IsReadOnly="True" BorderThickness="0" MouseDoubleClick="InvoiceRowsDataGrid_MouseDoubleClick">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="일자" Binding="{Binding InvoiceDateDisplay}" Width="110"/>
                                        <DataGridTextColumn Header="세금계산서" Binding="{Binding TaxInvoiceDisplay}" Width="110"/>
                                        <DataGridTextColumn Header="거래처" Binding="{Binding CustomerName}" Width="2*"/>
                                        <DataGridTextColumn Header="유형" Binding="{Binding VoucherTypeDisplay}" Width="70"/>
                                        <DataGridTextColumn Header="공급가" Binding="{Binding SupplyAmount, StringFormat=N0}" Width="100"/>
                                        <DataGridTextColumn Header="부가세" Binding="{Binding VatAmount, StringFormat=N0}" Width="90"/>
                                        <DataGridTextColumn Header="합계" Binding="{Binding TotalAmount, StringFormat=N0}" Width="100"/>
                                        <DataGridTextColumn Header="수금" Binding="{Binding PaidAmount, StringFormat=N0}" Width="90"/>
                                        <DataGridTextColumn Header="잔액" Binding="{Binding BalanceAmount, StringFormat=N0}" Width="90"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Border>

                            <GridSplitter Grid.Row="1" Height="8" Background="#0D1B2A"/>

                            <Border Grid.Row="2" Background="#1A2B4A" CornerRadius="6">
                                <DockPanel>
                                    <Border DockPanel.Dock="Top" Background="#102039" CornerRadius="6,6,0,0" Padding="10,7">
                                        <TextBlock Text="선택 전표 라인 미리보기" Foreground="#4FC3F7" FontWeight="Bold"/>
                                    </Border>
                                    <Border DockPanel.Dock="Bottom" Padding="10,8" BorderBrush="#2E4060" BorderThickness="0,1,0,0">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <TextBlock Text="공급가" Foreground="#9FB3C8" Margin="0,0,6,0"/>
                                            <TextBlock Text="{Binding PreviewSupplyAmount, StringFormat=N0}" Width="90" TextAlignment="Right"/>
                                            <TextBlock Text="원" Margin="2,0,16,0"/>
                                            <TextBlock Text="부가세" Foreground="#9FB3C8" Margin="0,0,6,0"/>
                                            <TextBlock Text="{Binding PreviewVatAmount, StringFormat=N0}" Width="80" TextAlignment="Right"/>
                                            <TextBlock Text="원" Margin="2,0,16,0"/>
                                            <TextBlock Text="합계" Foreground="#4FC3F7" FontWeight="Bold" Margin="0,0,6,0"/>
                                            <TextBlock Text="{Binding PreviewTotalAmount, StringFormat=N0}" Foreground="#4FC3F7" FontWeight="Bold" Width="100" TextAlignment="Right"/>
                                            <TextBlock Text="원" Foreground="#4FC3F7" Margin="2,0,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    <DataGrid ItemsSource="{Binding PreviewLines}" IsReadOnly="True" BorderThickness="0">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="품목" Binding="{Binding ItemName}" Width="*"/>
                                            <DataGridTextColumn Header="규격" Binding="{Binding Specification}" Width="120"/>
                                            <DataGridTextColumn Header="단위" Binding="{Binding Unit}" Width="70"/>
                                            <DataGridTextColumn Header="수량" Binding="{Binding Quantity, StringFormat=N0}" Width="80"/>
                                            <DataGridTextColumn Header="단가" Binding="{Binding UnitPrice, StringFormat=N0}" Width="100"/>
                                            <DataGridTextColumn Header="금액" Binding="{Binding LineAmount, StringFormat=N0}" Width="100"/>
                                            <DataGridTextColumn Header="비고" Binding="{Binding Remark}" Width="140"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </DockPanel>
                            </Border>
                        </Grid>

                        <GridSplitter Grid.Column="3" Width="8" Background="#0D1B2A"/>

                        <Border Grid.Column="4" Background="#1A2B4A" CornerRadius="6">
                            <DockPanel>
                                <Border DockPanel.Dock="Top" Background="#102039" CornerRadius="6,6,0,0" Padding="10,8"><TextBlock Text="거래처 정보" Foreground="#4FC3F7" FontWeight="Bold"/></Border>
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <StackPanel Margin="10,8">
                                        <TextBlock Text="상호" FontSize="11" Foreground="#9FB3C8"/>
                                        <TextBlock Text="{Binding PreviewCustomerName}" FontSize="16" Foreground="#4FC3F7" FontWeight="Bold" Margin="0,4,0,8"/>
                                        <TextBlock Text="사업자번호" FontSize="11" Foreground="#9FB3C8"/>
                                        <TextBox Text="{Binding EditCustBizNumber, UpdateSourceTrigger=LostFocus}" IsEnabled="{Binding HasSelectedCustomer}" Margin="0,4,0,8"/>
                                        <TextBlock Text="전화" FontSize="11" Foreground="#9FB3C8"/>
                                        <TextBox Text="{Binding EditCustPhone, UpdateSourceTrigger=LostFocus}" IsEnabled="{Binding HasSelectedCustomer}" Margin="0,4,0,8"/>
                                        <TextBlock Text="부서" FontSize="11" Foreground="#9FB3C8"/>
                                        <TextBox Text="{Binding EditCustDept, UpdateSourceTrigger=LostFocus}" IsEnabled="{Binding HasSelectedCustomer}" Margin="0,4,0,8"/>
                                        <TextBlock Text="담당자" FontSize="11" Foreground="#9FB3C8"/>
                                        <TextBox Text="{Binding EditCustContactPerson, UpdateSourceTrigger=LostFocus}" IsEnabled="{Binding HasSelectedCustomer}" Margin="0,4,0,8"/>
                                        <TextBlock Text="주소" FontSize="11" Foreground="#9FB3C8"/>
                                        <TextBox Text="{Binding EditCustAddress, UpdateSourceTrigger=LostFocus}" IsEnabled="{Binding HasSelectedCustomer}" TextWrapping="Wrap" Margin="0,4,0,8"/>
                                        <TextBlock Text="메모" FontSize="11" Foreground="#9FB3C8"/>
                                        <TextBox Text="{Binding EditCustNotes, UpdateSourceTrigger=LostFocus}" IsEnabled="{Binding HasSelectedCustomer}" Height="88" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </ScrollViewer>
                            </DockPanel>
                        </Border>
                    </Grid>
                </DockPanel>
            </TabItem>

            <TabItem Header="거래명세서[F9]">
                <Grid Margin="6">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Margin="0,0,0,10">
                        <WrapPanel Margin="0,0,0,6">
                            <Button Content="거래명세서 인쇄[F9]" Background="#1E88E5" FontSize="14" FontWeight="Bold" Padding="22,10" Command="{Binding PrintStatementCommand}" Margin="0,0,10,0"/>
                            <TextBlock Text="기본 내장 양식으로 인쇄합니다." VerticalAlignment="Center" Foreground="#9FB3C8"/>
                        </WrapPanel>
                    </StackPanel>
                    <Border Grid.Row="1" Background="#1A2B4A" CornerRadius="6">
                        <DataGrid ItemsSource="{Binding InvoiceRows}" SelectedItem="{Binding SelectedInvoiceRow}" IsReadOnly="True" BorderThickness="0" MouseDoubleClick="InvoiceRowsDataGrid_MouseDoubleClick">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="일자" Binding="{Binding InvoiceDateDisplay}" Width="110"/>
                                <DataGridTextColumn Header="세금계산서" Binding="{Binding TaxInvoiceDisplay}" Width="110"/>
                                <DataGridTextColumn Header="거래처" Binding="{Binding CustomerName}" Width="2*"/>
                                <DataGridTextColumn Header="유형" Binding="{Binding VoucherTypeDisplay}" Width="70"/>
                                <DataGridTextColumn Header="공급가" Binding="{Binding SupplyAmount, StringFormat=N0}" Width="100"/>
                                <DataGridTextColumn Header="부가세" Binding="{Binding VatAmount, StringFormat=N0}" Width="90"/>
                                <DataGridTextColumn Header="합계" Binding="{Binding TotalAmount, StringFormat=N0}" Width="100"/>
                                <DataGridTextColumn Header="수금" Binding="{Binding PaidAmount, StringFormat=N0}" Width="90"/>
                                <DataGridTextColumn Header="잔액" Binding="{Binding BalanceAmount, StringFormat=N0}" Width="90"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="회사 설정">
                <ScrollViewer Margin="6" VerticalScrollBarVisibility="Auto">
                    <Border Background="#1A2B4A" CornerRadius="6" Padding="16">
                        <StackPanel MaxWidth="680">
                            <TextBlock Text="회사 기본 정보" FontSize="18" FontWeight="Bold" Foreground="#4FC3F7" Margin="0,0,0,16"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="130"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Label Grid.Row="0" Grid.Column="0" Content="상호"/><TextBox Grid.Row="0" Grid.Column="1" Text="{Binding CompanyTradeName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                                <Label Grid.Row="1" Grid.Column="0" Content="대표자"/><TextBox Grid.Row="1" Grid.Column="1" Text="{Binding CompanyRepresentative, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                                <Label Grid.Row="2" Grid.Column="0" Content="사업자번호"/><TextBox Grid.Row="2" Grid.Column="1" Text="{Binding CompanyBusinessNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                                <Label Grid.Row="3" Grid.Column="0" Content="업태"/><TextBox Grid.Row="3" Grid.Column="1" Text="{Binding CompanyBusinessType, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                                <Label Grid.Row="4" Grid.Column="0" Content="종목"/><TextBox Grid.Row="4" Grid.Column="1" Text="{Binding CompanyBusinessItem, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                                <Label Grid.Row="5" Grid.Column="0" Content="주소"/><TextBox Grid.Row="5" Grid.Column="1" Text="{Binding CompanyAddress, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                                <Label Grid.Row="6" Grid.Column="0" Content="연락처"/><TextBox Grid.Row="6" Grid.Column="1" Text="{Binding CompanyContactNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                                <Label Grid.Row="7" Grid.Column="0" Content="이메일"/><TextBox Grid.Row="7" Grid.Column="1" Text="{Binding CompanyEmail, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                                <Label Grid.Row="8" Grid.Column="0" Content="계좌문구"/><TextBox Grid.Row="8" Grid.Column="1" Text="{Binding CompanyBankAccountText, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" Height="72" Margin="0,0,0,8"/>
                                <Label Grid.Row="9" Grid.Column="0" Content="직인 이미지"/>
                                <StackPanel Grid.Row="9" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="{Binding CompanyStampImagePath}" VerticalAlignment="Center" Foreground="#90CAF9" Margin="0,0,10,0"/>
                                    <Button Content="이미지 선택" Background="#37474F" Command="{Binding SelectStampImageCommand}" Margin="0,0,6,0"/>
                                    <Button Content="삭제" Background="#E53935" Command="{Binding ClearStampImageCommand}"/>
                                </StackPanel>
                            </Grid>
                            <Button Content="저장" Background="#1E88E5" FontSize="14" FontWeight="Bold" Padding="24,10" HorizontalAlignment="Left" Command="{Binding SaveCompanyProfileCommand}" Margin="0,12,0,0"/>
                        </StackPanel>
                    </Border>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
